#!/bin/sh
. "$(dirname -- "$0")/_/husky.sh"

echo "Running Next.js build before push..."
pnpm run build

if [ $? -ne 0 ]; then
  echo "Build failed! Push aborted."
  exit 1
fi

echo "Build succeeded! Pushing..."
